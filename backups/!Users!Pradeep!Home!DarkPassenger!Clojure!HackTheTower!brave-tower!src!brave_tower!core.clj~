(ns brave-tower.core)

(defn foo
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))

(type "asdf")
(type 10000)

(/ 22 4)

(- 1 2 3)
(type (- 1 2 2 (/ 3 4 9) (* 3 5 7)))
;; clojure executes inside out!!

(take 10 (range))

(def tired false)

(if (= tired true)
  (str "Time to sleep")
  (str "play with mac"))

[\a \b] ;; to define characters

;; list conj adds to the start of the array where as conj on vector adds to the end of the array

(list 1 2 3)
'(1 2 3 4)

;; get get-in to get vaules in maps
;; nth in list



